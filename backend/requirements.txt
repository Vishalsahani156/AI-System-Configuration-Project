# Core backend deps — safe to install anywhere (Render, cloud, no audio hardware).
# Offline voice engine libs live in requirements-offline.txt (need portaudio/espeak).
fastapi
uvicorn[standard]
pymongo
motor
pyotp
passlib[bcrypt]
python-jose[cryptography]
python-dotenv
