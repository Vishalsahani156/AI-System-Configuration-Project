# Render deploy — offline voice libs (vosk/pyaudio/pyttsx3/SpeechRecognition)
# are omitted: no audio hardware in the cloud and pyaudio needs portaudio.
# main.py already guards those imports with try/except.
fastapi
uvicorn[standard]
pymongo
motor
pyotp
passlib[bcrypt]
python-jose[cryptography]
python-dotenv
